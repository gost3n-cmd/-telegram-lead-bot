PROJECT BOOTSTRAP PROMPT

Ты работаешь как senior Python engineer и системный архитектор.

В проекте уже существует архитектурный документ:

stage1_architecture_v2.md

Этот документ является главным источником истины (source of truth).

Перед началом работы:

	1	Полностью прочитай stage1_architecture_v2.md.
	2	Не упрощай архитектуру.
	3	Не заменяй принятые решения своими аналогами.
	4	Если архитектурный документ противоречит твоим предпочтениям — следуй документу.
	5	Если чего-то нет в документе — сначала зафиксируй предположение в отдельном файле, а не внедряй его молча.

⸻

ГЛАВНОЕ ПРАВИЛО

У тебя может закончиться контекст или лимит ответа.

Поэтому после завершения КАЖДОГО значимого шага ты обязан сохранять результат в файлы проекта.

Нельзя хранить состояние проекта только в текущем чате.

Любое принятое решение должно существовать в проекте в виде файлов.

⸻

РЕЖИМ РАБОТЫ

Работа ведётся итерациями.

После каждой итерации:

	1	Обнови документацию.
	2	Сохрани прогресс в проект.
	3	Создай отчёт о проделанной работе.

Файл:

project_status.md

Структура:

	•	что реализовано
	•	какие файлы созданы
	•	какие файлы изменены
	•	что осталось сделать
	•	следующий рекомендуемый шаг

Этот файл обновляется после каждой итерации.

⸻

ДОКУМЕНТАЦИЯ

Создай каталог:

docs/

В нём поддерживай:

docs/implementation_progress.md
docs/decisions.md
docs/todo.md

Если лимит закончится, следующий запуск должен иметь возможность восстановить состояние проекта только по этим файлам.

⸻

ПРАВИЛО НЕПРЕРЫВНОСТИ

Перед началом любой новой задачи:

	1	Прочитай stage1_architecture_v2.md
	2	Прочитай project_status.md
	3	Прочитай docs/implementation_progress.md
	4	Прочитай docs/decisions.md

Только после этого продолжай работу.

⸻

ТЕКУЩАЯ ЦЕЛЬ

Реализовать Telegram Lead-Capture Bot строго по архитектуре из stage1_architecture_v2.md.

Технологии:

	•	Python 3.12+
	•	aiogram 3.x
	•	openpyxl
	•	asyncio
	•	dependency injection через main.py
	•	StorageInterface
	•	ExcelStorage
	•	FSM
	•	LeadService

Следовать архитектурным слоям из документа.

Запрещено:

	•	смешивать handler и storage логику
	•	обращаться к openpyxl из handler
	•	обходить LeadService
	•	ломать Sheet Ownership модель
	•	изменять build_system.py без необходимости

⸻

ПЕРВАЯ ЗАДАЧА

Сначала проведи аудит проекта.

Покажи:

	1	текущую структуру файлов
	2	какие элементы уже существуют
	3	какие отсутствуют относительно stage1_architecture_v2.md
	4	поэтапный план реализации

После этого начни реализацию с самого нижнего слоя архитектуры:

models
→ storage contracts

→ exceptions
→ lock
→ validation
→ services
→ handlers
→ main

После завершения каждого слоя обязательно:

	•	обновить project_status.md
	•	обновить docs/implementation_progress.md
	•	сохранить все изменения в файлы проекта

Если лимит ответа заканчивается — прекрати кодирование и сначала обнови документацию о текущем состоянии проекта.